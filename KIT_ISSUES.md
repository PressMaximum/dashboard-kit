# Kit issues

Living list of bugs / rough edges in `@pressmaximum/dashboard-kit` surfaced from
consumer integration. Different from [`docs/SPEC.md` §11](docs/SPEC.md) (the
"spike-hacks → kit fixes" roadmap) — this file tracks the **kit's own** defects
discovered when real consumers wire the public API.

Goal: catch issues once at the kit level + fix them in a single kit patch
release, instead of every consumer pasting the same workaround locally.

## How to use this file

When you find a kit bug while integrating, append a row to the table below:

- **Symptom** — what the user sees / what breaks. Concrete and reproducible.
- **Root cause** — where in the kit (file + line / class / API) the bug lives.
- **Consumer workaround** — the override the consumer is currently shipping
  to mask the bug. Cite the commit / file path so the workaround can be
  reverted once the kit fix lands.
- **Proper kit fix** — the change to land in the kit. Sketch the approach.
- **Target version** — kit version where the fix is expected to ship.
- **Discovered by** — consumer name + date so we can ping if the workaround
  pattern needs to evolve.

Keep entries short — link out to a kit PR / discussion thread for long
discussions. Each row gets a stable anchor (the **Issue** column key) so
consumer commit messages + workaround code comments can reference it.

## Open issues
> **Issue-number allocation:** numbers are claimed by the first row PUSHED to origin/main. Before using the next K-number, `git fetch` and re-check this file on origin/main — three parallel-session collisions happened on 2026-07-25 (K-023, K-030).


| Issue | Symptom | Root cause | Consumer workaround | Proper kit fix | Target | Discovered |
|---|---|---|---|---|---|---|
| `K-022` version-chip-prohibited-aria | `mountDashboard({ versionAriaLabel })` puts `aria-label` on a bare `<span class="pmdk-dashboard__version">` when no `versionHref` is given — the same `role="generic"` naming prohibition as K-020, so the documented prop silently does nothing for screen readers. (The `versionHref` branch renders an `<a>` and is fine.) Not in the Aponto axe report and not reachable from any story, because no consumer/story currently passes the prop. | [`src/core/DashboardShell.jsx:149`](src/core/DashboardShell.jsx) — `aria-label` on a generic element. | None needed — no consumer passes `versionAriaLabel`. | Needs a product call, not a mechanical fix: (a) ship a kit-owned visually-hidden utility class and render the context as hidden text — new public CSS surface; or (b) treat the visible version text as the name and deprecate the prop on the no-href branch — API semantics change. K-020's `role="img"` trick does not apply: the chip is text, not a glyph. | `0.2.x` | Kit K-020 audit, 2026-07-25 |
| `K-026` primitives-css-all-or-nothing | Packaging, not a defect (§5.12 documents the single stylesheet): the only way to get the module card's ~7.5 KiB of chrome is the whole ~128.7 KiB `primitives/style.css`. Measured on PressListing (first consumer): admin CSS 96 KiB → 202 KiB (**+103.1 KiB, +110%**) to render one component; the other ~121 KiB is combobox/inspector/table chrome the page never uses. | `primitives/style.css` is a single concatenated build artifact; no per-primitive CSS exports exist in the `exports` map. | Consumer accepts the full sheet (PressListing will, if K-023 unblocks first) or hand-extracts rules — the latter forks kit CSS and is not acceptable. | A granular `./primitives/module-card.css` export (and per-primitive sub-exports generally), keeping `./primitives/style.css` as the bundle. | `0.2.x` | PressListing P2.5 B6 module-card smoke-spike (first consumer), 2026-07-25 |
| `K-029` asset-enqueue-never-wires-rtl | Consumers' `*-rtl.css` files (emitted by wp-scripts for every stylesheet) are dead weight: in an RTL admin the LTR sheet still loads. PressListing measured it in its P2.5 code-split round — `build/dashboard-rtl.css` (and now `dataviews-tabs-rtl.css`) are produced on every build and never served. Affects every consumer that enqueues through the kit. | [`includes/Admin/AssetEnqueue.php`](includes/Admin/AssetEnqueue.php) registers the style handle but never calls `wp_style_add_data( $handle, 'rtl', 'replace' )` (and passes no `path` for `'suffix'` handling), so WP's RTL replacement machinery is never armed. | None shipped — PressListing left the gap alone (pre-existing, noted in its PR #29). A consumer could `wp_style_add_data` after the kit enqueue, but the handle timing is the kit's. | `AssetEnqueue` adds `wp_style_add_data( $handle, 'rtl', 'replace' )` when the sibling `*-rtl.css` exists beside the registered file (mirror core's pattern); document in ADOPTING that wp-scripts consumers get RTL for free. | `0.2.x` | PressListing P2.5 B8 code-split round, 2026-07-25 |

| `K-030` settings-store-no-saved-writer | `createSettingsStore` exposes load/edit/save/reset/clearDirty but NO targeted writer for the `saved` snapshot, and `load()` early-returns once `saved` is non-null (boot-seeded consumers can never re-seed). A consumer whose server state changes OUTSIDE the settings POST — PressListing's `PATCH /modules/{id}` persists into the same option — must fold the response into `saved` (NOT `dirty`: that lights the save bar + nav guard for an already-persisted edit) and has no public path to do it. | The factory publishes server snapshots only through its internal `LOAD_SUCCESS` action; no `receiveSaved`-style action is exported. | PressListing (P2.5 B7, shipped): decorate the store descriptor's `instantiate`, bind an extra action onto the instance action bag, dispatch the documented `LOAD_SUCCESS` with a merged payload. Works, no fork, but leans on the action name staying stable and on @wordpress/data descriptor internals. | A public `receiveSaved( partial )` action (shallow-merge into `saved`, dirty untouched) — the consumer deletes its decorator on bump. | `0.2.x` | PressListing P2.5 B7 Modules page (PATCH fold, AC-B5), 2026-07-25 |
| `K-032` module-card-description-clamp-overflows | The 2-line description clamp leaks the third line's ascenders: `.pmdk-module-description` sets `-webkit-line-clamp: 2` but also `min-height: 42px`, while `font-size: 12px` × `line-height: 1.5` = 18px/line ⇒ 2 lines = 36px. The forced 42px box lets the `-webkit-box` lay out a third line, whose top ~6px renders below the ellipsis. Measured on PressListing's Modules page (first consumer): 11 of 15 cards clipped (`scrollHeight` 54 / `clientHeight` 42); 4× zoom evidence captured in its B9 round. | [`src/primitives/module-card.css:165–175`](src/primitives/module-card.css) — `min-height: 42px` disagrees with the computed 2-line height (36px). | None shipped — a consumer override of a locked class is cascade-fragile; PressListing is waiting on the kit fix. | `min-height: 36px` (= 2 × 18px), or derive it from the type tokens so a future size change cannot desync again. VR the module-card stories at long-description fixtures. | `0.2.x` | PressListing P2.5 B9 browser walkthrough, 2026-07-25 |
| `K-033` module-grid-container-queries-never-match | `.pmdk-module-grid` never leaves 3-up: the responsive rules (`@container (max-width: 900px)` → 2-up, `(max-width: 620px)` → 1-up) are container queries, but NO ancestor of the grid declares `container-type` — the only `container-type: inline-size` declarations in the built primitives CSS are on `.pmdk-entity-preview` and `.pmdk-data-table`. With no query container the rules can never match. Measured on PressListing (first consumer): `cols=3` at 900/782/768/600/480px viewports, cards squeezed to 136px with colliding meta/badges and unreadable titles. | [`src/primitives/module-card.css`](src/primitives/module-card.css) — the `@container` rules ship without any element establishing the container for the grid's subtree. | Consumer could declare `container-type: inline-size` on its own wrapper — but which element is the intended container is a kit contract question, and self-declaring risks double-containment when the kit fixes it. PressListing left it 3-up. | Establish the container in the kit: `container-type: inline-size` on `.pmdk-module-grid`'s documented parent (or on the grid itself with the queries rewritten against it), document the required wrapper in SPEC §5.12, and VR the ModulesPage story at 900/620px. | `0.2.x` | PressListing P2.5 B9 browser walkthrough, 2026-07-25 |
| `K-034` dashboard-shell-nav-overflows-under-768 | The shell chrome overflows horizontally on narrow admin viewports on EVERY consumer tab: at 768px the header row (`.pmdk-dashboard__tabs` nav ≈563px + brand + `.pmdk-dashboard__version`) yields `scrollWidth` 780 vs `clientWidth` 768, pinned ≈780 down to 480px — the nav runs off-screen with no wrap/scroll affordance. Measured on PressListing's admin (6 tabs) in its B9 round; any consumer with ≥5 tabs reproduces. | [`src/core/DashboardShell.css`](src/core/DashboardShell.css) — the header row has no narrow-viewport strategy (no wrap, no overflow-x scroll, no priority collapse). | None shipped — PressListing noted it and moved on (wp-admin below 782px is rare but real: WP itself responsive-collapses there). | Pick a strategy at the kit level: horizontal scroll with fade affordance on `.pmdk-dashboard__tabs`, or wrap, or collapse-to-menu below a breakpoint; align with wp-admin's own 782px behavior; VR shots at 782/600/480. **PARTIAL FIX SHIPPED in the K-042 round (0.3, pending):** under `@media (max-width: 782px)` the header's tabs track is bounded (`auto minmax(0,1fr) auto`) and the nav scrolls inside it (`overflow-x: auto` + `flex: 0 0 auto` on the tabs) — the strip stays reachable instead of running off-screen, and nothing above 782px moves (VR 53/53 + 50/50 zero-diff). STILL OPEN: the fade affordance, the wrap-vs-collapse decision, and the 782/600/480 VR shots — the story matrix shoots one fixed 1280px viewport, so the narrow behaviour has no pixel guard yet. | `0.2.x` | PressListing P2.5 B9 browser walkthrough, 2026-07-25 |
| `K-036` theme-scope-doc-example-never-matches | The §16.1a HTML example (`<div class="pmdk-dashboard pmdk-theme-app">`) documents a placement that silently drops most of the theme: every component rule in the shipped `themes/app.css` is a DESCENDANT selector (`.pmdk-theme-app .pmdk-dashboard…` — 56/56 occurrences, zero compound forms), so the class ON `.pmdk-dashboard` misses the shell-canvas, `.page-title-action` and wide-main rules. Measured by PressListing (theme adoption round): shell font 16px with the class on an ancestor vs 13px with the doc's own example. | [`docs/SPEC.md` §16.1a](docs/SPEC.md) example vs the descendant-only selectors in [`src/themes/app.css`](src/themes/app.css). | PressListing places the class on its PHP mount node (the nearest consumer-owned ancestor) — works fully, themed at first paint. | Fix the §16.1a example to an ANCESTOR placement (and state the descendant-selector requirement), or add compound forms so ON-root also works. | `0.2.x` | PressListing P2.5 app-theme adoption, 2026-07-26 |
| `K-037` mount-has-no-root-classname-option | A JS-only consumer cannot opt into the app theme cleanly: `MountConfig` (§5.1) has no `className`/`rootClassName`, and `DashboardShell` renders `.pmdk-dashboard` with a locked class list — so the scope class can only come from markup the consumer happens to own ABOVE the mount (see K-036). A consumer whose PHP/mount markup is not theirs (or a pure-JS embed) has no sanctioned path. | [`src/core/DashboardShell.jsx`](src/core/DashboardShell.jsx) — fixed class list; `mountDashboard` passes no class through. | PressListing owns its mount node and put the class there (PHP-side). | `mountDashboard({ rootClassName })` forwarded onto the shell root (or a documented `themeClass` option), so the §16.1a opt-in is reachable from JS alone. | `0.2.x` | PressListing P2.5 app-theme adoption, 2026-07-26 |
| `K-038` theme-type-aliases-frozen-at-root | The app theme's headline "16px readable type" never lands: `--pmdk-font-size-body` and `--pmdk-font-size-field-value` are declared at `:root` ONLY as `var( --pmdk-font-size-base )`, and CSS substitutes a custom property's `var()`s against the element CARRYING the declaration — so both freeze to the root seed (14px) and inherit down as literals a theme scope below `<html>` can never move. The kit already knows this trap: its derived COLOUR roles are deliberately declared at `:root, .pmdk-theme-app` (with a comment saying why), and `themes/app.css` hand-patches `--pmdk-line-height-body` + `--pmdk-radius-control`; these two type aliases were simply missed. | [`src/styles/tokens.css`](src/styles/tokens.css) — the two aliases sit in the `:root`-only block. | PressListing re-substitutes both inside its theme scope (2 lines, marked `[KIT WORKAROUND]`, a no-op once the kit fixes). | Declare the two aliases at `:root, .pmdk-theme-app` like the colour roles (and audit the token sheet for any other `var()`-carrying alias declared `:root`-only). | `0.2.x` | PressListing P2.5 app-theme adoption, 2026-07-26 |
| `K-039` theme-misses-portal-surfaces | Portalled `@wordpress/components` surfaces render UNTHEMED inside a themed admin: `Modal`/`ConfirmDialog` portal to `document.body` — outside any `.pmdk-theme-app` scope — and `themes/app.css` ships no `.components-modal*` rules at all, so passing the scope class via the component's `className` changes nothing (the tokens resolve at `:root` values out there). Measured on PressListing's §6.5 disable dialog: 13px/8px-radius WP-native chrome over a 16px/6px themed page. Snackbars share the risk. | [`src/themes/app.css`](src/themes/app.css) — no portal-surface coverage; the scope-class model cannot reach `document.body` portals by itself. | None clean — PressListing ships the mismatch (dialog stays WP-native). | Theme the portal surfaces: either `.components-modal__frame` etc. rules keyed off a body-level marker the kit sets when the theme is active, or document `__unstableSlotName`/frame-class recipes per surface. | `0.2.x` | PressListing P2.5 app-theme adoption, 2026-07-26 |
| `K-040` theme-base-voice-misses-components-text | `@wordpress/components` `Text`/`Heading` primitives keep their emotion-injected 13px under the theme's 16px chassis — visible as card descriptions and row copy one step smaller than surrounding body text. Minor (reads fine) but the §16.1a "DS base voice" claim doesn't reach the most common text primitive consumers actually use. | emotion runtime styles carry component-level `font-size: 13px` with class-hash specificity the theme's element rules don't target. | None shipped — PressListing accepts the mismatch. | Extend the theme's component rules to the `Text`/`Heading` class hooks (`.components-text` etc.) inside the scope, at specificity that clears emotion's (see K-035's precedent… note the emotion-tie mechanics documented in PressListing's BaseLabel workaround). | `0.2.x` | PressListing P2.5 app-theme adoption, 2026-07-26 |
| `K-041` shell-lacks-overflow-appearance-menu | The header's kebab (⋮) overflow — Enter full screen + Appearance (Light / Dark / System) — exists only as Aponto-local composition; a second consumer wanting the same chrome must rebuild it. The dark side is especially kit-shaped: the kit OWNS the dark preset (`data-pmdk-color-scheme="dark"` retones every derived role) but ships no control that sets/persists it, so each consumer invents its own scheme state, persistence, and System-preference tracking. | No overflow-menu or appearance-control surface in `MountConfig`/`DashboardShell` (§5.1 — help + version are the only header extras). | PressListing ships WITHOUT the kebab (follow-the-kit rule: no local rebuild of shell chrome); Aponto carries its local version. | Promote the Aponto pattern (the module-card/K-018 path): a shell overflow slot or `menuItems`-style MountConfig option, plus a kit-owned appearance control (scheme state + `prefers-color-scheme` tracking + persistence + the `data-pmdk-color-scheme` write). | `0.3.x` | PressListing P2.5 follow-the-kit round (founder direction), 2026-07-26 |
| `K-044` dropdown-link-trigger-escape-reopens | On a K-042 dropdown tab with a LINK trigger, Escape does not close the menu: `closeAndRestore()` closes then moves focus to the trigger, and the wrapper's focus-disclosure immediately re-opens it — probed on the first consumer: after Escape, `aria-expanded` is back to `true` with focus on the trigger. Not a focus trap (blur/pointer-out still close), but Escape is dead, which fails the menu-button pattern's own contract. | The focus-open condition does not distinguish "focus arriving by user intent" from "focus restored by our own Escape handler" — the restore re-triggers disclosure. | None consumer-side (the dance is inside the kit component). PressListing ships with the quirk noted. | Suppress the disclosure for the focus event synthesized by `closeAndRestore()` (a one-shot guard flag or `focus({ preventScroll })` + ignore-next-focus marker), and add the Escape case to the TabStrip unit suite for BOTH trigger modes. | `0.3.x` | PressListing kit-0.3.0 adoption round, 2026-07-26 |
| `K-045` dropdown-children-never-active | K-042 dropdown child rows never receive `aria-current`: `DashboardShell` passes `activeRoute` as the matched route TEMPLATE (e.g. `#settings/:panelId`), so a child `href` like `#settings/payments` never equals it. The parent trigger lights correctly (first-segment id match); the open menu shows no current row. The kit's own story sidesteps it by registering STATIC per-panel routes — a consumer with a parameterized route cannot. | Child active-match compares against the route template instead of the LIVE hash. | Consumer could register static routes per panel (route-table churn; rejected by the first consumer) or live with no current row (shipped). | Match children against the current location hash (or pass both template and resolved hash down and prefer the hash for child comparison); unit-test with a parameterized route. | `0.3.x` | PressListing kit-0.3.0 adoption round, 2026-07-26 |
## Process notes

Not consumer-facing bugs — repo mechanics worth writing down once.

- **Committed `build/` can go stale (K-019 follow-up).** `build/` is tracked
  because Aponto consumes the kit as a git dependency and gets those files
  verbatim, and `tests/integration/prebuilt-entries.test.jsx` asserts their
  contract. That test therefore checks whatever artifact is committed, not the
  current `src/`. Two mitigations are in place: CI now runs **Build before
  Test**, so the pipeline always exercises fresh output, and a following
  advisory step runs `git diff --exit-code -- build/` to flag a rebuild the
  author forgot to commit. The advisory step is `continue-on-error: true` for
  now because it also compares minifier output between the runner's Node 20 and
  whatever Node built the committed files — rebuilds are byte-identical on one
  machine and the sourcemaps are path-portable (`webpack://`, no absolute
  paths), but cross-Node stability has not been proven. Flip it to blocking
  once a few runs are clean. Locally: run `npm run build` before committing any
  `src/` change.

## Closed issues

| Issue | Symptom | Fix | Shipped in | Consumer revert |
|---|---|---|---|---|
| `K-046` primitives-tier-ships-no-border-box-reset | **The systemic one — parent of K-047…K-051's sibling symptoms.** Every primitives-tier box rendered inflated by its own padding + border, because the whole tier was extracted from the Aponto mockup's `.pd-*` layer whose stylesheet opens (line 1 of `plugin-dashboard.css`) with a GLOBAL `*{box-sizing:border-box}` — and the extraction took the rules but not the reset. The authored numbers only ever meant border-box, so every one of them was wrong by exactly its own chrome. Measured on PressListing's Modules page (first consumer, 1440×900) and reproduced in a two-sheet harness: toggle track authored **38×22 → rendered 44×28** (+64% area), `.pmdk-module-card` 302→340, `.pmdk-module-phase` authored `min-width:28` → **42×23**. *Discovered: PressListing P2.5 QA round, 2026-08-01.* | A scoped reset at the top of [`base.css`](src/primitives/base.css): `.pmdk-dashboard *, .pmdk-dashboard *::before, .pmdk-dashboard *::after { box-sizing: border-box }`. **Scope is the whole design of the fix (REVISED-C).** It lives in the PRIMITIVES tier, which is its own webpack entry (`primitives/style` → `build/primitives/style.css`): core consumers (Blocksify, Customify) import `style.css` from the `index` entry, whose module graph never reaches this file, so their box model cannot move. Verified structurally, not just argued — after the change `git diff origin/main -- build/style.css build/themes/app.css` is EMPTY (only `build/primitives/style.css` + the module-card entry changed), and the story VR shows **all 21 core-default / theme-app-core shots zero-diff**. Descendants only: the chassis element is core's to style (and carries no padding or border either way). Measured after: toggle track **44×28 → 38×22**, card height 270→250, phase badge **42×23 → 29.1×21** — every box now paints the size it was authored at. | `0.3.1` (pending) | None for core-only consumers — they never load this sheet, and their pixels are provably unchanged. Consumers of `primitives/style.css` should drop any local `box-sizing` patch or hand-tuned size override they added to compensate; the tier now measures as documented. Primitives-tier VR baselines legitimately move (25 re-captured in this PR). |
| `K-047` module-card-toggle-labels-too-long | `<PMDKModuleCard>`'s default toggle labels were `Enabled` / `Disabled`, which the card's own chrome never expected: `.pmdk-module-toggle-label` reserves `min-width: 20px` — a two-to-three character gutter — and the mockup the card was extracted from renders **On** / **Off**. The long words blew the label cluster out to **46.4px** (56px at PressListing's type scale), stealing width from the footer action beside them. *Discovered: PressListing P2.5 QA round, 2026-08-01.* | `DEFAULT_LABELS` in [`PMDKModuleCard.jsx`](src/module-card/PMDKModuleCard.jsx) → `toggleOn: 'On'`, `toggleOff: 'Off'`. Measured: label cluster **46.4px → 20px** (exactly the `min-width` floor the CSS always encoded). 2 unit cases pin the pair in both states + 1 pins that the verbose form is still reachable. SPEC §5.12 `labels` row documents the change and the restore path. | `0.3.1` (pending) | **Default TEXT change — the one consumer-visible behaviour change in this round.** A consumer that wants the verbose wording passes `labels={ { toggleOn: 'Enabled', toggleOff: 'Disabled' } }`; translated consumers already pass `labels` and are unaffected. |
| `K-048` modules-counts-strip-recipe-points-at-drawer-chrome | The SPEC §5.12 Modules-page recipe and the `ModulesPage` story both told consumers to build the counts strip with `.pmdk-mini-stats` — which is the DRAWER's metric-tile chrome ([`drawer.css`](src/primitives/drawer.css)): a bordered 3-column grid of boxed tiles. Consumers who followed the recipe (PressListing did) got a **460×57** slab of hard chrome sitting above a card grid that needs none. The mockup's actual Modules-page strip is a different surface entirely, and the kit never ported it. *Discovered: PressListing P2.5 QA round, 2026-08-01.* | Ported `.pd-module-overview` from the mockup into [`module-card.css`](src/primitives/module-card.css) as **`.pmdk-module-overview`** (+ its `span` / `b` / `i` children): a compact inline flex run — counts in `<b>`, `·` separators in `<i>`, `font-variant-numeric: tabular-nums` — plus the source's narrow-container column stack. Measured: **460×57 boxed tiles → a 15px-tall inline run**. Font size comes from the kit's own `--pmdk-font-size-caption` (12px) per the §16.5 neutralisation rule — the mockup's 13px is Aponto's value for that same token, not a hard-coded size. `.pmdk-mini-stats` is deliberately UNTOUCHED and stays the drawer surface. SPEC §5.12 recipe snippet + §16.5 row updated; `ModulesPage` story rewired. | `0.3.1` (pending) | Consumers following the old recipe should swap `.pmdk-mini-stats` for `.pmdk-module-overview` markup (shape in §5.12). Nothing breaks if they don't — `.pmdk-mini-stats` still renders, it is just the wrong surface for this page. The strip's `@container` rule inherits the **K-033** limitation (no ancestor establishes a query container), so the narrow stack cannot match until K-033 lands. |
| `K-049` module-icon-glyph-14px-not-31px | The module-card icon rendered at **13×13** inside its 34px box — the glyph inherited the card's body font-size, because K-035 sized it at `1em` and nothing set the em. The mockup renders it at **31×31** (`.pd-module-glyph{width:31px;height:31px}` in a 34px cell), so every consumer's card had a visibly undersized icon floating in a large empty cell. **This is the follow-up K-035 explicitly filed and deferred** ("the v4 mockup sizes this glyph at 31px… raising it would move every existing consumer's card, i.e. a look change for a minor, not a patch"). *Discovered: PressListing P2.5 QA round, 2026-08-01.* | `font-size: 31px` on `.pmdk-module-icon` in [`module-card.css`](src/primitives/module-card.css). Deliberately on the BOX, not the `<svg>`: K-035's `> svg, > * > svg { width: 1em }` then resolves to 31px for **every** wrapper a consumer might pass, so the wrapper-independence K-035 bought is preserved rather than special-cased. Measured: glyph **13×13 → 31×31**, icon box unchanged at **34×34**. | `0.3.1` (pending) | None — additive; consumers that hand-sized the glyph to compensate can drop the override. **Judgement call worth the reviewer's attention:** K-035 deferred this precisely because it moves every card, and this ships it in a PATCH. The justification is that 0.3.1 is a visual-CORRECTION round where the primitives tier moves anyway (K-046 re-baselines 24 shots regardless), so absorbing it here costs one look-change instead of two. If the orchestrator would rather this waited for a minor, it is a one-line revert. |
| `K-050` module-card-badges-starve-title | `.pmdk-module-card-head`'s third track was `auto`, so it took the badge cluster's max-content width — and since every badge is `white-space: nowrap`, that width had no ceiling. A wide product badge squeezed the copy column from 206px to 80px on PressListing's page; in the reproduction harness a three-badge cluster crushed it to **0px**, ellipsing titles that had room to spare. *Discovered: PressListing P2.5 QA round, 2026-08-01.* | Two halves, both in [`module-card.css`](src/primitives/module-card.css). (1) The track becomes **`fit-content(40%)`** — chosen over `minmax(auto,40%)` because `fit-content()` still resolves to max-content whenever max-content fits under the cap, so the short-badge path is untouched rather than merely "close". Verified by isolation: re-measuring the fixed sheet with the OLD `auto` track restored gives the copy column **163px either way** — the clamp moves nothing for short badges, while the wide case goes **0px → 138.4px**. (2) `flex-wrap: wrap` + `row-gap` on `.pmdk-module-badges`, so a clamped cluster wraps instead of overflowing (and its automatic minimum drops to the widest SINGLE badge, which is what lets the clamp bind). Badge cluster measured **300.9×23 unbounded → 128.3×73 wrapped**. New `LongBadgeCluster` story (pathological + short cluster side by side) added to the VR matrix; 3 unit cases pin the track, the wrap and the row gap. | `0.3.1` (pending) | None — additive. Short-badge cards (every shipped consumer today) render identically **from this change**; they do move slightly in this release, but from K-046's reset shrinking the badges themselves (copy column 150.1→163px), not from the clamp. |
| `K-051` primitives-buttons-missing-pointer-cursor | Kit buttons read as inert text — no pointer cursor on `.pmdk-button`, `.pmdk-icon-button`, or `.pmdk-section-tabs button`. Same extraction gap as K-046: the mockup relies on a bare `button{cursor:pointer}` ELEMENT rule, which carried no `.pd-` selector for the mechanical rename to catch, so it was silently dropped along with the box-sizing reset. *Discovered: PressListing P2.5 QA round, 2026-08-01.* | `cursor: pointer` on the three selectors ([`buttons.css`](src/primitives/buttons.css) ×2, [`tabs.css`](src/primitives/tabs.css) ×1). Added per-class rather than as a scoped `.pmdk-dashboard button` element rule, so the kit does not silently restyle a consumer's own buttons that happen to live inside the chassis. Swept the rest of the tier for the same miss: menu items, row actions, status pickers, filter/toolbar/table/pagination/column-manager controls, bulk bar, combobox and settings-shell rail nodes **all already declare it** (39 pre-existing `cursor` declarations audited) — these three were the only gaps. `.pmdk-button:disabled`'s `not-allowed` still wins (later in the cascade), pinned by a unit case. Measured: `default → pointer` on all three. | `0.3.1` (pending) | None — additive, and cursor is not a screenshot-visible property, so **zero VR impact**. Consumers shipping a local `cursor: pointer` patch on kit buttons can drop it. |
| `K-052` module-card-statuslabel-cannot-coexist-with-toggle | `statusLabel` rendered ONLY in the branch where the toggle is ABSENT — `showToggle ? <toggle/> : <span>{ isPlanned ? plannedLabel : statusLabel }</span>` in [`PMDKModuleCard.jsx`](src/module-card/PMDKModuleCard.jsx). So a LIVE toggleable card could never show a static footer note through the slot designed for it: PressListing's Modules page needed a transient "Reload to apply" marker beside an enabled module's toggle and had to fall back to stuffing it into the `action` slot, which is the deep-link/Configure surface, not a status surface. *Discovered: PressListing P2.5 QA round (consumer half), 2026-08-01.* | `statusLabel` now COEXISTS with the toggle: when passed on a card that renders a control, it emits a quiet `<span class="pmdk-module-toggle-label pmdk-module-status-note">` immediately BEFORE the toggle cluster — same typography as the planned/status span it generalises, and outside the `<label>` so clicking the note can never flip the control. New `.pmdk-module-status-note` rule carries `margin-left: auto` so the note and the toggle share the footer's trailing edge instead of the note being stranded beside the action. **Absent-by-default (REVISED-C):** the node is emitted only when `statusLabel` is truthy, so every card without one renders byte-identical DOM and no default baseline moves — verified, the 26 pre-existing module-card/primitives shots stayed zero-diff on the re-run. `planned` keeps `plannedLabel` and gains no second node. 3 unit cases (coexistence + ordering/containment, the absent-by-default DOM, the planned branch) + a `ToggleWithStatusNote` story pairing a card with the note against the same card without one. SPEC §5.12 prop row + §16.5 class row. | `0.3.1` (pending) | None — additive and opt-in. Consumers currently routing a status note through `action` can move it to `statusLabel` and get the intended placement + typography. |
| `K-043` settings-shell-primitive-missing | The founder-look Settings layout — full-bleed chrome (rail flush to the content edge), a LEFT NAV TREE with grouped/collapsible sections, sticky SaveBar — existed only as Aponto-local `pd-*`/`ap-*` composition. The kit's `SchemaForm` renders a single vertical column and `SubNav` a flat rail, so the second consumer could not follow the look without rebuilding the whole shell locally. PressListing's SPEC-P2.5 §10 recorded it as "[OPEN]: a kit settings-shell issue or compose locally"; its founder resolved it 2026-07-26 — FOLLOW THE KIT. | New sub-entry [`@pressmaximum/dashboard-kit/settings-shell`](src/settings-shell/index.mjs) (mirrors `module-card/`: own chunk so `./primitives` stays React-free, zero third-party deps, chrome in `primitives/style.css`). **`<SettingsShell>`** — grid rail + padded content column, named `role="region"`, `header`/`children` slots, three geometry knobs published as CSS custom properties (`chromeOffset` 96px / `railWidth` 240px / `contentMaxWidth` 876px). **`<SettingsNav>`** — new `.pmdk-settings-nav*` family, `.pmdk-subnav*` untouched (locked classes AND the `<2 items ⇒ null` rule, which is wrong for a settings rail); Aponto's contract promoted verbatim: parent click SELECTS ITS FIRST CHILD (one interaction, never expand-vs-select), arrows move FOCUS not selection (activating on arrow would fire a consumer's dirty-form confirm per keypress), `aria-controls` only while the group exists, caret on the individual `rotate` property because rtlcss negates `transform: rotate()` (K-031) with RTL re-pointing CLOSED branches only. **`createSettingsTree()`** — Aponto's ia.js resolvers generic-ized (no tree literal, no legacy aliases, no domain fields; consumer fields ride on `section().source`), every resolver TOTAL so a stale bookmark lands on a real section. **`containerWidth: 'flush'`** — additive third mode on `DashboardShell` (`narrow`/`wide`/unknown resolve exactly as before). Collapse follows the **K-033** lesson: the shell establishes the query container and the GRID is its child, so the ≤820px rule has something to match — no orphan `@container` block, and any container-queried primitive composed inside gets a container for free. Evidence: **VR 55/55 pre-existing story shots + 50/50 mockup Gate-A shots zero-diff** (`primitives/style.css` gaining `settings-shell.css` moved nothing), 6 new baselines, 49 unit cases across [`SettingsShell`](tests/unit/SettingsShell.test.jsx) / [`SettingsNav`](tests/unit/SettingsNav.test.jsx) / [`createSettingsTree`](tests/unit/createSettingsTree.test.js) (the tree cases ported from the Aponto suite this was promoted from) + a prebuilt-entry contract row; 2.24 KB gzip against a 3 KB size-limit. SPEC §5.14 · §5.10b · §16.2 · §16.5 · §4.1. | `0.3.x` (pending) | None — additive, and PressListing can drop its `SubNav` + vertical `SchemaForm` stand-in. **Two scope cuts, deliberate:** (a) NO `rows: 'horizontal'` SchemaForm variant — recon proved Aponto production never renders label-left rows (mockup-only), so shipping a layout variant no consumer has designed against would freeze a guess into the schema API; deferred until a consumer designs it. (b) NO `SchemaBuilder` PHP change — in v1 the tree is consumer-declared JS config; schema-driven grouping is noted as future work in §5.14. |
| `K-042` tabstrip-cannot-split-primary-and-utility-nav | `TabStrip` modelled ONE flat ordered run: no alignment, no grouping, no per-tab dropdown. Aponto's header IA — primary destinations left, utility surfaces (`Modules · Settings ▾`) right — was therefore unreachable through the kit tab system, and reproducing it meant recomposing the header outside `TabStrip` and forfeiting its active-sync, a11y and theme rules (which is what Aponto's local header does). PressListing wanted the same split and shipped all six tabs flat. | Two optional `TabDefinition` fields, both additive. **`align: 'start'│'end'`** partitions the strip into a start run and an end run *inside the same `<nav>` landmark*; `TabStrip` marks the nav `data-has-end` and [`DashboardShell.jsx`](src/core/DashboardShell.jsx) marks its root `data-utility-tabs`, and the header grid template swap lives behind that root marker — required because the default `1fr auto 1fr` header sizes the centre track to its CONTENT, so no auto-margin inside the nav can reach the right edge. **`children: [{ id, label, description?, href? }]`** renders the tab as a dropdown on `@wordpress/components` `<Dropdown>` (the `HelpPanel` precedent — focus management, click-outside, Escape). Trigger shape comes from the tab's own `hash`: present ⇒ a real LINK that also discloses on hover/focus and never click-toggles; `hash: ''` ⇒ a click-toggle BUTTON (an explicit empty hash now survives `mountDashboard`'s `toTabShape`, the one-line change that made the button form reachable). `aria-haspopup="menu"` + `aria-expanded`, `role="menu"`/`"menuitem"`, `aria-current="page"` on the active child AND its parent trigger; ArrowDown/Up from the trigger opens onto the first row, Arrow/Home/End rove, Escape closes and restores focus. **Deliberate deviation from the promoted source:** Aponto also paints the panel from CSS (`:hover / :focus-within { display:block !important }`) — the origin of its Escape edge case; here hover/focus feed the SAME state the click path uses, so DOM and ARIA cannot disagree. Zero new translatable strings (the menu is named by its trigger via `aria-labelledby`), so `TabStrip` stays Tier-1. Evidence: **VR 53/53 story shots + 50/50 mockup Gate-A shots zero-diff** (with no `align`/`children` the component emits the historical markup — no group wrappers, no marker attributes), 19 cases in [`tests/unit/TabStrip.test.jsx`](tests/unit/TabStrip.test.jsx), 2 new baselines (`core-tabstrip--split-nav`, `--utility-dropdown`). SPEC §5.1 · §5.10b · §16.1a · §16.2. | `0.3.x` (pending) | None — additive. A consumer that passes neither field renders byte-identically; PressListing can move `Modules` + `Settings` to `align: 'end'` and drop the flat-six layout, and Aponto can retire its local header composition. Pairs with **K-034**: this round also bounded the header's tabs track below 782px (partial mitigation, noted on that row). |
| `K-035` module-icon-glyph-unsized-for-foreign-wrapper | `<PMDKModuleCard icon={…} />` rendered NO visible icon unless the consumer's icon node happened to carry the kit's own `.pmdk-react-icon` wrapper class. Aponto's Modules page hit it; any product with its own icon renderer will. | [`module-card.css`](src/primitives/module-card.css) sized the 34px BOX (`.pmdk-module-icon`) but never the glyph — the only thing sizing the `<svg>` was `.pmdk-react-icon > svg { width: 1em }` in [`base.css`](src/primitives/base.css), which `defaultRenderIcon` supplies. A foreign wrapper handed the grid cell an `<svg>` with nothing but a `viewBox`, and a viewBox-only SVG in a `display: grid; place-items: center` cell resolves to **0x0** — measured in-browser, so the icon was invisible rather than merely small. | Two selectors give every direct child the same 1em treatment: `.pmdk-module-icon > svg, .pmdk-module-icon > * > svg { width: 1em; height: 1em }`. One wrapper deep on purpose (a descendant selector would reach into future icon-adjacent chrome). Browser measurement, same story, both wrappers: foreign **0x0 → 14x14**, kit `.pmdk-react-icon` **14x14 → 14x14 unchanged**. 6 unit cases pin the cascade (3 fail on 0.2.3); `NonToggleableIntegration` story gains a foreign-wrapper card. VR 53/53 zero diff. | `0.2.4` (pending) | None — additive. Consumers that hand-sized the glyph to work around this can drop the override; consumers already passing `defaultRenderIcon` output see no change. | **Follow-up worth a founder call:** the v4 mockup sizes this glyph at **31px** in the 34px box (`.pd-module-icon .pd-settings-glyph{width:31px}`), but the kit has always rendered **14px** (1em) for BOTH wrapper paths. This fix deliberately did not close that gap — raising it would move every existing consumer's card, i.e. a look change for a minor, not a patch. Filed here so it is not lost. |
| `K-031` rtl-double-flip-and-direction-lock | The kit ships LTR stylesheets only — every consumer's build runs rtlcss over them to emit `*-rtl.css` — and two constructs in `primitives/style.css` were booby traps there. **(a)** 6 rules keyed on `.pmdk-dashboard[dir=rtl]`: dead, because direction comes from `<html dir="rtl">` and the app root carries no `dir` attribute, AND rtlcss mirrors their declarations a SECOND time, so had the scope ever gained the attribute they would have rendered LTR inside an RTL page. **(b)** 5 `direction: ltr` rules pinning money / time / date / id runs: rtlcss rewrites the VALUE, so the RTL build emitted `direction: rtl` and flipped exactly the runs the rules exist to pin. Same defect class Aponto's RTL pass found in its own mockup CSS. | **(a)** deleted all 6 ([`drawer.css`](src/primitives/drawer.css) ×2, [`table.css`](src/primitives/table.css) ×2 + one redundant selector, [`status.css`](src/primitives/status.css) ×1) — each mirrored a base rule rtlcss already flips correctly, verified per-rule: the RTL build's effective declarations are IDENTICAL before and after, so this is pure dead-code removal. None was an RTL-only rule needing an html-level rescope (no `scaleX` mirrors in the kit). **(b)** new **`--pmdk-dir-lock: ltr`** token declared on `.pmdk-dashboard` in [`tokens.css`](src/styles/tokens.css) AND [`primitives/base.css`](src/primitives/base.css), with all 5 sites reading `direction: var( --pmdk-dir-lock )` — rtlcss never rewrites custom-property DECLARATIONS, which is the escape hatch. Declared in both sheets because rtlcss DOES rewrite `var()` fallbacks (`var( --x, ltr )` → `var( --x, rtl )`), so a fallback is not available and no sheet may rely on another being loaded. Also audited `transform: rotate()`: all 3 uses are 180°, where rtlcss's negation renders identically — documented inline so a future non-symmetric angle moves to the individual `rotate:` property. Evidence: real rtlcss 4.3.0 over the built sheets — `direction: rtl` in the RTL build **5 → 0**, kit-scoped `[dir=rtl]` rules **6 → 0**, LTR output functionally unchanged (VR 53/53 zero diff). Guarded by [`tests/unit/rtlSafety.test.js`](tests/unit/rtlSafety.test.js) (8 cases, 5 fail on 0.2.2). | `0.2.3` (pending) | None — LTR rendering is unchanged and RTL consumers get correct output with no action. Consumers with their own `direction: ltr` rules should adopt the same token: `direction: var( --pmdk-dir-lock )`. Pairs with **K-029** (`AssetEnqueue` never arms `wp_style_add_data( …, 'rtl', 'replace' )`) — until that lands, a consumer enqueuing through the kit never SERVES the corrected RTL sheet. |
| `K-023` module-card-integration-forces-toggle | **Blocked PressListing P2.5.** A non-toggleable integration could not render: outside the `planned` state `<PMDKModuleCard>` always shipped a tab-reachable checkbox labelled "Disable Stripe" that did nothing — with `onToggle` omitted the DOM checkbox flipped and React reverted it. A dead control AT announces as actionable. | New **`toggle`** prop (default `true`, so every existing consumer is byte-identical) in [`PMDKModuleCard.jsx`](src/module-card/PMDKModuleCard.jsx): `false` renders the footer `action` slot plus a new optional **`statusLabel`** — the same footer shape `planned` produces — and no control at all, not a hidden-but-focusable one. Deliberately decoupled from `state`, so a connected integration keeps `is-enabled` chrome and its place in the product's on/available counts. Chose `toggle` over `toggleable`/`showToggle` as the shortest name that reads as the slot it gates. §5.12 prop table + emitted `.d.ts` + a `NonToggleableIntegration` story case; 3 unit tests (the main one fails on 0.2.1). | `0.2.2` (pending) | None needed — additive. PressListing's `stripe` row passes `toggle={ false }` + `statusLabel` and drops the `state:'planned'` workaround it refused to ship. |
| `K-024` toggle-aria-node-title-stringifies | §5.12 typed `title` as `node`, but a node title produced the toggle accessible name `"Disable [object Object]"` — silently breaking the "every toggle carries an accessible name" contract in the same section. Overriding `labels.toggleAria` did not help: the override received the same node. | New **`titleText`** prop ([`PMDKModuleCard.jsx`](src/module-card/PMDKModuleCard.jsx)) supplies the plain-text name and `toggleAria` prefers it. Two defences behind it: the resolved name falls back to `title` only when `title` is already a string, and the default `toggleAria` omits the name entirely when empty — so a node title with no `titleText` announces "Disable" rather than stringifying the element. `labels.toggleAria( name, enabled )` now receives the RESOLVED string, so consumer overrides are safe too. Contract narrowed in §5.12: `title` stays `node`, `titleText` is required when it is not a string. 3 unit tests; the name assertion fails on 0.2.1 with the exact `'Disable [object Object]'`. | `0.2.2` (pending) | None — consumers passing string titles are unaffected. Consumers marking up titles add `titleText`. |
| `K-025` tier-badge-is-free-unreachable | `primitives/style.css` shipped a green "Free" tier badge (`.pmdk-module-license.is-free`) no consumer could reach: `tier={{ label:'Free' }}` rendered a bare `class="pmdk-module-license"`. Dead chrome. | **Deviation from the proposed fix, with evidence.** The row preferred emitting `is-free` whenever `isPremium` is falsy; implemented that way it diverged **4 VR baseline shots (144–310 diff px)** — `{ label: 'Free' }` is what shipped consumers already pass, so the green tint would restyle all of them on a PATCH bump, against the kit's zero-look-change promise. Took the row's own alternative instead: **`tier.variant: 'free' │ 'premium'`** ([`PMDKModuleCard.jsx`](src/module-card/PMDKModuleCard.jsx) `tierVariantClass()`), opt-in — `'premium'`/`isPremium: true` → `is-premium`, `'free'` → `is-free`, omitted → bare badge exactly as today. `.is-free` is reachable and documented (§5.12 + §16.5) with VR 53/53 zero diff. If the green-by-default look is wanted, it is a deliberate look change for 0.3, not a patch. | `0.2.2` (pending) | None — additive and pixel-neutral. A consumer wanting the free badge passes `tier={{ label: 'Free', variant: 'free' }}`. |
| `K-028` sticky-action-cell-traps-row-menu | An open row-action menu was painted over by the action cells of the rows BELOW it — the trailing ~26–78px of the popover, truncating item labels ("Duplicate a…"). Pre-existing since the table tier shipped; surfaced during Aponto's adoption. | `.pmdk-col-action` is `position: sticky; z-index: 3`, so **every action cell is a stacking context** and a menu inside one is confined to its CELL's level in the paint order — the menu's own `z-index` (18 anchored / 120 floating) only orders things INSIDE the trap, and later rows' opaque sticky cells (same z-index, later in tree order) win. [`table.css`](src/primitives/table.css) now raises the one cell that owns the open menu: `.pmdk-col-action:has( [aria-expanded='true'] ) { z-index: 35 }` — matching the existing `.pmdk-status-picker.is-open` idiom, above sibling cells (3) and the sticky header (4), below drawer (70) and floating menus (120). Attribute-keyed, so it follows the menu state with no JS change; `aria-expanded` is set by `createMenu` and by the documented consumer pattern. `:has()` is already in the kit's browser floor. Evidence: `elementFromPoint` sampling of the open menu's own box in Chromium — 551 sampled points owned by another row's cell, reaching 78px deep, → **0 points**; plus 5 unit assertions in [`tests/unit/stickyActionStacking.test.js`](tests/unit/stickyActionStacking.test.js) (3 fail on 0.2.1) and the `Table/PMDKDataTable → StickyActionMenuStacking` story. | `0.2.2` (pending) | None — pure z-index raise on a state that was previously unusable. Consumers that hacked around it (raising the cell themselves, or moving row menus out of the action column) can drop that. |
| `K-027` dnd-kit-peer-range-too-narrow | The optional peers pinned `@dnd-kit/sortable: ^8.0.0` (and `@dnd-kit/core: ^6.3.1`, `@tanstack/react-table: ^8.21.3`), so a consumer on a newer dnd-kit major hit `ERESOLVE` and had to add npm `overrides` — npm enforces peer ranges even for `optional` peers once the package is present. | Ranges widened in [`package.json`](package.json) to the majors the kit's actual API surface supports, verified against the registry: `@dnd-kit/sortable` `>=8 <11` (`SortableContext`, `useSortable`, `arrayMove`, `sortableKeyboardCoordinates`, `verticalListSortingStrategy` are unchanged across 8/9/10 — the only published majors), `@dnd-kit/core` `>=6 <7` (6.x is the newest major; only `DndContext`, `closestCenter`, `PointerSensor`, `KeyboardSensor`, `useSensor(s)` are used), `@dnd-kit/utilities` `>=3 <4` (only `CSS`), `@tanstack/react-table` `>=8 <9` (no v9 exists; the six imports are the v8 core surface). Documented as a range table in [ADOPTING §2](docs/ADOPTING.md). | `0.2.2` (pending) | **Consumers can delete their `overrides` / `resolutions` entries for `@dnd-kit/*` and `@tanstack/react-table`.** Note `@dnd-kit/sortable` still carries its own peer on `@dnd-kit/core` (v10 wants `^6.3.0`), which constrains the pairing independently of the kit. |
| `K-019` table-prebuilt-inlines-prod-jsx-runtime | Consuming the prebuilt `build/table/index.mjs` under a DEV React (WP `SCRIPT_DEBUG`) fired "unique key" warnings on kit static-children arrays (false positives — the source markup is correct), and duplicated `@tanstack/react-table` + `dnd-kit` for any consumer that also bundled them. | Root cause was broader than the row: **all four React entries** (`index`, `datasets`, `table`, `module-card`) inlined a **production** `react/jsx-runtime` (`r.jsx=s,r.jsxs=s` — one implementation for both exports), so kit elements never carried the dev runtime's static-children marker and a dev React re-validated them as dynamic arrays. [`webpack.config.js`](webpack.config.js) now externalizes `react/jsx-runtime` + `react/jsx-dev-runtime` (wp-scripts maps them to the `react-jsx-runtime` handle) and `@tanstack/react-table` + `@dnd-kit/core|sortable|utilities`; [`package.json`](package.json) moves those four from `dependencies` to **optional `peerDependencies`** (also devDeps, so the kit still builds/tests/stories them). Built table entry 127,256 → 26,020 raw bytes; 37.9 → 8.49 KB gzip. New [`tests/integration/prebuilt-entries.test.jsx`](tests/integration/prebuilt-entries.test.jsx) (10 cases) imports the COMMITTED `build/` artifacts with the peers resolved externally, asserts no entry embeds a runtime or third-party copy, and renders the prebuilt table under a dev React with zero console output — the pre-fix artifact reproduced `Warning: Each child in a list should have a unique "key" prop` from inside `build/table/index.mjs` on the same render. | `0.2.1` (pending) | **Consumer contract change** — a consumer importing `@pressmaximum/dashboard-kit/table` must now provide `@tanstack/react-table@^8.21.3` + `@dnd-kit/core@^6.3.1` + `@dnd-kit/sortable@^8` + `@dnd-kit/utilities@^3.2.2` itself (Aponto already declares all four). Aponto then drops the B4b webpack aliases (`…/table$`, `…/module-card$` → `src/`) and the scoped babel rule; `react-jsx-runtime` stays in `admin.asset.php`. |
| `K-020` prohibited-aria-attributes | Axe flagged ~25 serious `aria-prohibited-attr` on Aponto admin: `.pmdk-active-filters` and `.pmdk-compare__check-yes`/`__check-no` carried `aria-label` on bare `div`/`span` (role `generic`, which prohibits naming), so screen readers dropped the name or announced it inconsistently. | Labeled per what the markup means, not by stripping attributes. [`CompareTable.jsx`](src/compare/CompareTable.jsx) boolean cells get `role="img"` — they are glyphs standing in for a word, and `img` permits naming with presentational children so the icon/em-dash can't be announced on top. [`PMDKDataTable.jsx`](src/table/PMDKDataTable.jsx) `.pmdk-active-filters` **and** `.pmdk-filter-builder` (same defect, only reachable with the panel open, so not in the report) get `role="group"` — named sets of filter widgets, the idiom `<SchemaForm>` already uses. Deliberately NOT `<span class="screen-reader-text">`: the kit does not ship that CSS (it borrows wp-admin's), so hidden text would render visible outside wp-admin and break the VR gate. New [`tests/unit/a11y-prohibited-attr.test.jsx`](tests/unit/a11y-prohibited-attr.test.jsx) runs real axe-core scoped to the rule over both surfaces plus the `activeFilters` / open-panel states no story reaches; `axe-core` added as a devDependency. Real-browser axe over the affected stories: 18 → 0 violations, VR 53/53 unchanged. | `0.2.1` (pending) | None — no consumer had a workaround. Aponto's axe report loses these ~25 serious findings on the next bump. Residual sibling tracked as K-022. |
| `K-021` fixed-menu-ignores-containing-block | `createMenu( root, { position: 'fixed' } )` positioned its popover from `trigger.getBoundingClientRect()` against `window.innerWidth/innerHeight`. Any ancestor that is a containing block for fixed descendants (`transform`, `filter`, `contain: layout`, …) made those viewport coordinates resolve against the ancestor's padding box instead, so the menu landed offset — measured 25px across and 282–534px down in a real browser. Consumers could not put such a wrapper anywhere above a fixed menu. | [`src/primitives/menu.js`](src/primitives/menu.js): a computed-style walk **nominates** the nearest ancestor that may reparent fixed descendants (transform family, perspective, filter/backdrop-filter, `contain: layout\|paint\|content\|strict`, `container-type`, `content-visibility`, matching `will-change`), then `resolveContainingBlock()` **probes** the engine at three points — `left/top: 0` plus both basis vectors 100px along each axis — and accepts the candidate only when the popover starts on its padding-box origin (±1px, popover margin accounted for) and each axis moves 1:1 with what is written. An affine map is fixed by exactly those three images, so only a pure translation passes. Confirmed blocks get the origin subtracted on every reposition; everything else keeps the pre-fix arithmetic untouched. Every probe matters: Chromium 149 does **not** reparent for `container-type`, so a spec-derived property list alone would have MOVED container-query menus that were never broken, and a scaled / rotated / skewed / perspective-projected block would be half-corrected by a subtraction that cannot undo a bent coordinate system — it is rejected instead. Same change also moves fixed-mode geometry + scroll/resize listeners onto `root.ownerDocument.defaultView`, so an iframed dashboard is clamped against its own viewport (no-op for same-realm consumers). Public API, DOM contract and `.d.ts` unchanged; uncontained popovers keep byte-identical `left/top` (verified before/after in Chromium 149 + unit assertions), and the 53-shot Storybook VR matrix is unchanged and zero-diff. | `0.2.1`; chain-walk completion `0.2.2` (pending) | None — no consumer shipped a workaround. Unblocks putting `container-type` / `transform` / `contain` wrappers above kit menus; Aponto can enable its shell container query and drop the 820px viewport fallback. **Round 2 (0.2.2):** the 0.2.1 search stopped at the FIRST nominated ancestor, so a chain like `transform` shell → `container-type` table → menu nominated the table, failed the origin check and applied NO correction while the engine really had reparented onto the shell (proven in Aponto adoption: menus off by the transform ancestor's origin). The walk now resumes past a rejected candidate and adopts the first one the engine confirms; the probe stays candidate-independent, so it still runs at most once per open. |
| `K-012` hero-border-token-gap | Kit's `.pmdk-hero` shipped a literal `border: 0`; consumers wanting a hairline to match surrounding cards had to override the class with a full border declaration in every product. | Tokenised (KIT-P4): [`src/styles/tokens.css`](src/styles/tokens.css) Hero block gains `--pmdk-hero-border: 0`; [`Hero.css`](src/welcome/Hero.css) reads `border: var( --pmdk-hero-border, 0 )`. Computed default unchanged (`0`) — Blocksify gate zero-diff. | `0.2.0` (pending tag) | Blocksify swaps its `.bsy-dashboard-welcome .pmdk-hero { border: … }` class override for the 1-line token `.bsy-dashboard-welcome { --pmdk-hero-border: 1px solid var( --pmdk-color-border, #e0e0e0 ); }` (either keeps rendering — the class override still wins while it exists). |
| `K-013` card-heading-flush-recipe | `@wordpress/components` Emotion `:first-of-type`/`:last-of-type` rules round all four `CardHeader` corners, detaching the heading from a `<Checklist>` below; the chained-class override that defeats the specificity had to be rediscovered per consumer. | Documented (KIT-P4): SPEC §10 Card-wrap recipe now carries the flush-heading snippet (`.consumer-scope .components-card-header.components-card__header { border-bottom-left-radius: 0; border-bottom-right-radius: 0; }`, specificity (0,3,0) > Emotion's (0,2,1)) + §16.3 pitfall note. `<ChecklistCard>` wrapper deliberately NOT shipped (K-002 "kit owns no chrome"). | `0.2.0` docs (pending tag) | None — Blocksify keeps its override; it is now the documented recipe. |
| `K-014` compare-cta-wraps-button | Long CTA description in `<CompareTable>`'s upsell banner forced `flex-wrap` to drop the upgrade button below the text (auto flex-basis = content width). Customify shipped shortened copy to dodge it. | [`CompareTable.css`](src/compare/CompareTable.css) `.pmdk-compare__cta-text` → `flex: 1 1 0; min-width: 0` (KIT-P4): text grows into remaining space, button keeps its right seat, long copy wraps within the text column. Short copy renders pixel-identical (story + Blocksify shots zero-diff). | `0.2.0` (pending tag) | Customify Theme restores the original ~150-char FreeVsPro description (drops the K-014 NB comment). |
| `K-015` snackbar-not-centered | Transient snackbars rendered bottom-left: `align-items: center` on the slot is a no-op against `<SnackbarList>`'s full-width block notice-container. | [`DashboardShell.css`](src/core/DashboardShell.css) now ships `.pmdk-dashboard__snackbar .components-snackbar-list__notice-container { display: flex; justify-content: center; }` (KIT-P4). | `0.2.0` (pending tag) | Blocksify drops its identical scoped rule in `src/dashboard/style.css` (redundant now — computed unchanged either way). |
| `K-017` list-empty-hides-filter-toolbar | A persisted filter/search matching zero rows unmounted the whole DataViews UI (toolbar included) — no way to clear the filter; reload restored the trap. | `hasActiveQuery` gate in [`EntityListPage.jsx`](src/datasets/EntityListPage.jsx) (shipped `628ab78`): the bare empty state only renders when NO filter/search is active; under an active query `<DataViews>` stays mounted with its toolbar + reset. KIT-P4 closes the row + adds 3 regression tests (`tests/unit/EntityListPage.test.jsx`). | `0.2.0` (pending tag) | None viable existed; stuck users no longer need the localStorage-key wipe. |
| `K-018` module-card-primitive-missing | **Consumer request** (PressListing P2.5 Modules page): the kit shipped no module-card primitive, so PressListing would be the second product hand-rolling the Aponto `pd-module-*` card anatomy — per-consumer duplication + drift. | New **`<PMDKModuleCard>`** sub-entry [`@pressmaximum/dashboard-kit/module-card`](src/module-card/PMDKModuleCard.jsx) (own entry keeps `./primitives` React-free; zero third-party deps) on the extracted [`module-card.css`](src/primitives/module-card.css) chrome, following the `PMDKDataTable` split — kit ships behavior + chrome + slots (icon · meta line · title · tier chrome `.pmdk-module-license` + badge slot `.pmdk-module-phase` · description · integration-state line `.pmdk-module-connection` · footer action slot + canonical toggle with `onToggle`/`toggleDisabled`; states `enabled`/`disabled`/`planned`, planned = no toggle + product-injected static label; a11y: no whole-card click, accessible toggle names, tier ≠ runtime status); product keeps data + copy. Grid/category-tabs/counts-strip stay product-side — SPEC §5.12 recipe + the `ModuleCard/ModulesPage` story. API: SPEC §5.12; 8 unit tests + 6 stories + VR shots. | `0.2.0` (pending tag) | None needed — PressListing consumes the new entry; Aponto swaps its mockup-local classes at KIT-P5 adoption. |
| `K-001` color-cascade-pin | Tab/SubNav/version/CURRENT-pill/SaveBar-dirty all paint with `:root`-level `--wp-admin-theme-color` (legacy `#007cba`) instead of body-level scheme (`#3858e9` on modern fresh). | [`src/styles/tokens.css`](src/styles/tokens.css) moves the `--pmdk-color-accent` declaration off `:root` to `body.wp-admin, .pmdk-dashboard` so it resolves at a cascade level that sees the body-level `--wp-admin-theme-color`. Other tokens (`text`, `bg`, `border`, etc.) stay at `:root`. | `0.1.0` | Drop the K-001 override block in `src/dashboard/style.css` (Blocksify commits [`c04b6b6`](https://github.com/pressmaximum/blocksify/commit/c04b6b6), [`e944d92`](https://github.com/pressmaximum/blocksify/commit/e944d92)). |
| `K-002` checklist-double-chrome | Kit's `<Checklist>` ships `border + background + border-radius` on `.pmdk-checklist`, conflicting with the component's own docstring ("consumer wraps in Card"). Consumers that compose `<Card><Checklist/></Card>` get visible double-card nesting. | [`src/welcome/Checklist.css`](src/welcome/Checklist.css) drops `background`, `border`, `border-radius` from `.pmdk-checklist`. Kit keeps semantic `<section>` + a11y label; chrome belongs to the consumer's surrounding `<Card>` (or any other wrapper they choose). | `0.1.0` | No revert needed — Blocksify's `<Card>` wrap is now the single chrome. |
| `K-003` hero-defaults-bare | Kit's `.pmdk-hero` used a generic white + 1px border + 4px radius + 24px title look, losing the WP `about.php`-distilled feel the kit was originally designed to ship. Welcome surfaces read as a thin form instead of a hero band. | [`src/welcome/Hero.css`](src/welcome/Hero.css) restores: `padding: 48px`, `background: #ececec` (matches WP's `about.__header`), `border: 0`, `border-radius: 8px`, `.pmdk-hero__title { font-size: 28px }`, `.pmdk-hero__tagline { margin: 8px 0 24px; font-size: 15px; max-width: 56ch }`. Layout primitives (flex direction, mobile breakpoint) unchanged. | `0.1.0` | Blocksify's `.bsy-dashboard-welcome-hero*` rules were dropped during the P2 migration; the kit now ships the same look natively. No re-add needed. |
| `K-004` compare-thin-look | `.pmdk-compare__head-cell` + `__section-title` + `__row` styles diverged from the Blocksify-distilled rich look. Section titles rendered as bare text (no tinted band), rows had per-cell padding instead of per-row inset, head row used 12px/0.04em letterspacing instead of 11px/0.08em uppercase. | [`src/compare/CompareTable.css`](src/compare/CompareTable.css) re-aligns head/section-title/row + CTA banner. Per-row `padding: 14px 24px` on `__head + __row` (replacing per-cell padding). Section title gets `background: var(--pmdk-color-bg-subtle)`, `font-size: 11px`, `letter-spacing: 0.08em`, `text-transform: uppercase`. CTA banner gets the cream `#fffbf0` + yellow `#f0d57c` hairline border. New `.pmdk-compare__rows` wrapper class for flex row stack. | `0.1.0` | Blocksify's `.bsy-dashboard-compare*` rules were dropped during P4 migration; kit now ships the rich look natively. |
| `K-005` schema-form-no-spacing | `<SchemaForm>` rendered fields back-to-back with only their `@wordpress/components` margins between them. No spacing rule on `.pmdk-schema-form` meant consumers re-added their own `.consumer-settings__fields { gap: 20px }` wrapper. | New [`src/settings/SchemaForm.css`](src/settings/SchemaForm.css) imports from `SchemaForm.jsx`. Adds `.pmdk-schema-form { display: flex; flex-direction: column; gap: 20px }` so the kit ships the visual baseline by default. Locked-class status documented inline (public surface from 1.0). | `0.1.0` | Blocksify dropped `.bsy-dashboard-settings__fields { gap: 20px }` during P3 migration; the kit's pmdk-schema-form rule replaces it. |
| `K-006` category-badge-left-align | `.pmdk-category-badge` is `display: inline-flex` with `min-width: 72px` but no horizontal centering. Short labels (`NEW`, `FIX`, `NEW`) hug the left edge of the pill, looking off-balance next to the variable-width pill chrome. | [`src/changelog/CategoryBadge.css`](src/changelog/CategoryBadge.css) adds `justify-content: center` to `.pmdk-category-badge`. The min-width still makes the pill visually consistent; the label now sits at the optical centre regardless of length. | `0.1.0` | No consumer change needed — kit fix only. |
| `K-007` sparse-token-surface | Component-level visual properties (Hero padding/bg/radius, Compare row inset + section-title bg + CTA bg, Checklist row padding, SubNav bg + radius, ReleaseBlock padding + radius, generic card radius) were hardcoded literals in component CSS files. Consumers wanting a different look had to override class targets via cascade overrides — drift risk + per-consumer duplication. | [`src/styles/tokens.css`](src/styles/tokens.css) appends 16 component-level tokens (`--pmdk-hero-*`, `--pmdk-compare-*`, `--pmdk-checklist-*`, `--pmdk-subnav-*`, `--pmdk-release-block-*`, `--pmdk-radius-card`). Component CSS files (`Hero.css`, `Checklist.css`, `CompareTable.css`, `SubNav/editor.css`, `ReleaseBlock.css`) replace hardcoded literals with `var()` lookups that fall back to the current defaults. Public token surface grows ~16 entries; pre-1.0 add is non-breaking. | `0.1.0` | No consumer change needed — kit defaults match consumer expectation. Customify Theme / future consumers override tokens (5-10 lines of CSS in `.pmdk-dashboard` or `:root`) instead of class overrides. |
| `K-008` schema-options-silent-array-coerce | `SchemaBuilder::selectField()` + `radioField()` silently coerced array option values to literal string `"Array"` + raised `PHP Warning: Array to string conversion` when consumers passed the verbose `[ ['value' => 'v', 'label' => 'V'], ... ]` shape instead of the documented `value => label` map. Schema still saved but every option rendered as the word "Array". | New `SchemaBuilder::normalizeOptions()` private helper coerces both input shapes into the canonical `{value, label}[]` array. The map form (`'v' => 'V'`) still works for terse cases; the shaped form (`['value' => 'v', 'label' => 'V']`) now passes through unchanged. Both `selectField()` + `radioField()` delegate to the shared helper. Docblock on both methods updated to document the two accepted shapes. | `0.1.0` | No consumer revert needed — Customify session shipped the map-form workaround inline; either shape works after this fix. |
| `K-009` brand-href-accepted-but-ignored | `mountDashboard({ brand: { href } })` was documented in SPEC §5.1 but never consumed by `DashboardShell` — the brand `<h1>` rendered without any anchor wrap or click target. Consumers wanting the brand mark to navigate had to ship DOM-level click delegation themselves. | [`src/core/DashboardShell.jsx`](src/core/DashboardShell.jsx) now reads `brand?.href` + `brand?.ariaLabel`. When `href` is present the brand content wraps in `<a class="pmdk-dashboard__brand-link">` with `onClick={ useNavigate()(href) }`, so internal hash targets route through `HashRouter` instead of triggering a full reload. Mirrors the existing `versionLabel + versionHref` pattern. | `0.1.0` | Customify Theme dropped its `wireBrandClick()` DOM delegation in [P16](https://github.com/PressMaximum/customify/commit/bce66e06) once `brand.href` was honoured. |
| `K-010` heading-weights-shouty | Kit primitives shipped heading weights at 600 across `.pmdk-hero__title`, `.pmdk-checklist__label`, `.pmdk-release-block__version`. Combined with consumer card heads inheriting the same heavy default, Welcome pages read as a wall of bold with no internal hierarchy. No font-weight tokens shipped, so consumers had to override each class explicitly. | New font-weight token tier in [`src/styles/tokens.css`](src/styles/tokens.css): `--pmdk-font-weight-heading: 500` (hero title, release-block version), `--pmdk-font-weight-label: 400` (checklist label). Component CSS in [`Hero.css`](src/welcome/Hero.css), [`Checklist.css`](src/welcome/Checklist.css), [`ReleaseBlock.css`](src/changelog/ReleaseBlock.css) replaces hardcoded `font-weight: 600` literals with `var()` lookups. Consumers rebalance at the root via the two new tokens instead of per-class overrides. | `0.1.x` | Customify Theme drops `.pmdk-checklist__label { font-weight: 400 }` override in `dashboard-v2.scss`; keeps theme-class overrides on `.customify-dashboard-*-title` selectors (kit doesn't own those). |
| `K-011` save-bar-idle-conflated-with-saved | `<SaveBar>` rendered three states off two booleans; the idle state used the same green-check + "All changes saved" copy + green `--pmdk-color-success` colour as a freshly-completed save. On first page load this read as a confirmation of a save the user never performed. Reset button stayed enabled regardless of dirty state. | Three changes in [`src/settings/SaveBar.jsx`](src/settings/SaveBar.jsx) + [`src/settings/SaveBar.css`](src/settings/SaveBar.css): **(a)** default `statusSaved` label flips to neutral `"No pending changes"`; **(b)** `.pmdk-save-bar__status.is-saved` swaps colour from `--pmdk-color-success` to `--pmdk-color-text-muted` so the cue matches the neutral wording — consumers wanting the celebratory cue dispatch their own snackbar from `onSave`; **(c)** new opt-in prop `resetDisabledWhenNotDirty` (default `false`) lets consumers gate the Reset button per-state for "discard dirty edits" semantics. Backward-compatible: existing consumers see no behaviour change unless they pass the new prop. | `0.1.x` | Customify Theme drops its `statusSaved` label overrides in `Settings.jsx` + `ProModuleSettingsPanel.jsx` and the `.pmdk-save-bar__status.is-saved` colour override in `dashboard-v2.scss`. The Pro panel keeps its `handleDiscard()` short-circuit + snackbar (theme-side UX choice, not a workaround). |
| `K-016` main-keyboard-focus-ring | When the user keyboard-Tabs into `.pmdk-dashboard__main`, the browser paints its default `:focus-visible` outline (WP admin accent `#3858e9`, ~2 px) around the entire main wrapper — typically 1100×1400+ px on a regular admin window — producing a giant blue rectangle around the whole content area. | [`src/core/DashboardShell.css`](src/core/DashboardShell.css) drops the `:not( :focus-visible )` qualifier so the existing focus-suppression rule becomes `.pmdk-dashboard__main:focus { outline: none }` (now covers keyboard focus too). The SPA route-change `focus()` call still moves focus to the landmark for screen-reader announcement; the visual ring on a page-wide wrapper had no a11y value. Optional follow-up if a visible keyboard focus cue is wanted later: shift the SPA's `focus()` target to the active panel heading inside `__main` and add a custom rule on that smaller landmark. | `0.1.x` | No consumer revert needed — neither Customify Theme nor Blocksify Free had shipped an override. |
