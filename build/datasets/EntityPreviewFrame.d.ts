export default function EntityPreviewFrame({ src, previewDoc, title, viewportWidth, viewportHeight, aspectMode, itemId, onContentHeight, emptyLabel, className, }?: {
    viewportWidth?: number;
    viewportHeight?: number;
    aspectMode?: string;
    emptyLabel?: string;
}): import("react").JSX.Element;
