/**
 * PMDK Test Consumer — dashboard entry.
 *
 * P0 scaffold: empty. P1 will replace this with a real mountDashboard()
 * call against the kit's core module once it ships.
 *
 * Run during development:
 *   1. From repo root: `npm link`
 *   2. From this dir:   `npm link @pressmaximum/dashboard-kit`
 *   3. Symlink this plugin folder into a local WP install's
 *      wp-content/plugins/ and activate.
 *   4. Edit kit src/ — rebuild — refresh WP admin.
 */

// Placeholder so wp-scripts has something to compile during P0.
// eslint-disable-next-line no-console
console.log( '[pmdk-test-consumer] P0 scaffold loaded' );
